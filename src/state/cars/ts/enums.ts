export enum ECarsActionTypes {
  WS_TRY_CONNECT = '[Cars] WS Try Connect',
  WS_CONNECTED = '[Cars] WS Connected',
  WS_DISCONNECTED = '[Cars] WS Disconnected',
  WS_ERROR = '[Cars] WS Error',
  WS_MESSAGE_SEND = '[Cars] WS Message Send',
  WS_MESSAGE_SENT = '[Cars] WS Message Sent',
  WS_MESSAGE_RECEIVED = '[Cars] WS Message Received'
}
